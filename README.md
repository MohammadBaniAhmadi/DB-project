# DB-project---Data-Pipeline-from-OLTP-to-OLAP
The objective of this project is to design and implement a data pipeline for transferring transactional sales data from an operational database (OLTP) to an analytical database (OLAP). The project primarily focuses on modularizing the processes of Change Data Capture (CDC), incremental data loading, and efficient query execution.
